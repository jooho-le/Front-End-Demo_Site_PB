version: 2
updates:
  - package-ecosystem: "github-actions"
    directory: "/"
    schedule:
      interval: "weekly"

# npm/yarn updates are disabled until a package manifest is added.
# Re-enable by adding an `npm` entry here once package.json exists.
